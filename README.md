compile to a binary or use as is.


`flowtime start` start a running timer
`flowtime break` stop the timer, and calculate a break
`flowtime time` focused time spent so far
`flowtime stop` quit the timer
